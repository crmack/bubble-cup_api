<h1><%= @player.name %></h1>

<p>
  <strong>Years Active:</strong>
  <% @player.teams.each do |team| %>
    <%= team.bubble_cup.year %>
  <% end %>
  <% @captains.each do |captain| %>
    <%= captain.bubble_cup.year %>
  <% end %>
  
  <strong>Years Captain:</strong>
  <% @captains.each do |captain| %>
    <%= captain.bubble_cup.year %>
  <% end %>
</p>


<li><%= link_to "Edit", edit_player_path(@player) %></li>

<li><%= link_to "Destroy", player_path(@player), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %></li>

<%= link_to "All Players", players_path %>
